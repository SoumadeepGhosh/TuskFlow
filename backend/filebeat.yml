filebeat.inputs:
  - type: filestream
    enabled: true

    paths:
      - /usr/share/filebeat/logs/taskflow.log

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"

setup.template.enabled: true

logging.level: info